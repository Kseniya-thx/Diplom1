@using Diplom1.Models;
@model  IEnumerable<Diplom1.Models.Depart>


<div class="row">



</div>

<h2>Отделы</h2>

<form asp-action="Index" method="get" >
    <div class=" form-group">
        <label for="SearchString">Поиск</label>
        <input class="text"  name="SearchString"  value="@ViewData["Filtr"] "/>
        <input type="submit" value="Поиск" class="btn btn-primary" />
        <!--<div class="input-group-addon">-->
            @*<button type="submit" class="btn btn-primary">Поиск</button>*@
        <!--</div>-->
    </div>
</form>
@Html.ActionLink("Добавить", "Create" )
<table class="table table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name_Depart)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name_Depart)
                </td>
                <td>
                    @Html.ActionLink("Редактировать", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Удалить", "Delete", new { id = item.Id })
                </td>

            </tr>
        }
    </tbody>
</table>